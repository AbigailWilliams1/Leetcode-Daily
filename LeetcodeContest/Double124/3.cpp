#include "temp.h"

class Solution {
public:
    int maxOperations(vector<int>& nums) {
        // int n = nums.size(), l = 0, r = n-1, ans = 0;
        // int sum = nums[l] + nums[l+1];
        // while()
        
    }
};